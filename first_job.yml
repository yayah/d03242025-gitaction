name: First Workflow

on:
  push:
    branches:
      - main

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Welcome Message
        run: echo "My first GitHub Actions Job"

      - name: List Files
        run: ls

      - name: Read file
        run: cat compose.yml